/** Shared top-bar height for dashboard header and sidebar brand row. */
export const DASHBOARD_TOP_BAR_CLASS = "h-14 sm:h-16";

/** Pointer cursor for interactive dashboard controls (links, buttons, labels). */
export const DASHBOARD_INTERACTIVE_CLASS =
  "[&_a[href]:not([aria-disabled=true])]:cursor-pointer [&_button:not(:disabled)]:cursor-pointer [&_[role=button]:not([aria-disabled=true])]:cursor-pointer [&_label[for]:not([aria-disabled=true])]:cursor-pointer";
